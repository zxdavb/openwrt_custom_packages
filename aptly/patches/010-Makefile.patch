--- a/Makefile
+++ b/Makefile
@@ -29,7 +29,7 @@ ifeq ($(RUN_LONG_TESTS), yes)
 endif
 
 install:
-	go install -v -ldflags "-X main.Version=$(VERSION)"
+	(cd $(GOPATH)/src/github.com/aptly-dev/aptly && go install -v -ldflags "-X main.Version=$(VERSION)")
 
 system/env: system/requirements.txt
 ifeq ($(RUN_LONG_TESTS), yes)

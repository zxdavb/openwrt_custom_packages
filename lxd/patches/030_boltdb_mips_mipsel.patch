--- /dev/null
+++ b/github.com/boltdb/bolt/bolt_mips.go
@@ -0,0 +1,12 @@
+// +build mips
+
+package bolt
+
+// maxMapSize represents the largest mmap size supported by Bolt.
+const maxMapSize = 0x3FFFFFFF // 1GB
+
+// maxAllocSize is the size used when creating array pointers.
+const maxAllocSize = 0xFFFFFFF
+
+// Are unaligned load/stores broken on this arch?
+var brokenUnaligned = false
--- a/Makefile
+++ b/Makefile
@@ -13,6 +13,8 @@ TAGS=$(shell printf "\#include <sqlite3.
 
 .PHONY: default
 default:
+	go get -d github.com/boltdb/bolt
+	mv -u github.com/boltdb/bolt/* ../../boltdb/bolt
 	go get -t -v -d ./...
 	go install -v $(TAGS) $(DEBUG) ./...
 	@echo "LXD built successfully"
--- /dev/null
+++ b/github.com/boltdb/bolt/bolt_mipsle.go
@@ -0,0 +1,12 @@
+// +build mipsle
+
+package bolt
+
+// maxMapSize represents the largest mmap size supported by Bolt.
+const maxMapSize = 0x3FFFFFFF // 1GB
+
+// maxAllocSize is the size used when creating array pointers.
+const maxAllocSize = 0xFFFFFFF
+
+// Are unaligned load/stores broken on this arch?
+var brokenUnaligned = false
